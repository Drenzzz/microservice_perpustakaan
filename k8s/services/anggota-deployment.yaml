apiVersion: apps/v1
kind: Deployment
metadata:
  name: anggota-service
  labels:
    app: anggota-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: anggota-service
  template:
    metadata:
      labels:
        app: anggota-service
    spec:
      containers:
        - name: anggota-service
          image: anggota-service:latest
          imagePullPolicy: Never # Use local image (built by docker build)
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: microservices-config
          env:
            - name: SERVER_PORT
              value: "8081"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:h2:mem:anggota;DB_CLOSE_ON_EXIT=FALSE"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 90
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: anggota-service
  labels:
    app: anggota-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
  selector:
    app: anggota-service
